# Governed Coding Agent

Example mission:
- objective: investigate a failing build and prepare a patch
- safe tools: read_file, search_files
- risky tools: terminal, write_file, patch

Why it matters:
- coding agents often need exactly the risky surface that scares platform and security teams
- MissionLedger can make the authority boundaries and approval steps explicit
